// 1. Declaración de Función
function example(a, b, c) {
  return a + b + c;
}

// 2. Función de Expresión
const example = function (a, b, c) {
  return a + b + c;
};

// 3. Función de Expresión - Arrow Function
const example = (a, b, c) => a + b + c;
